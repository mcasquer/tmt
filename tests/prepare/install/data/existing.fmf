summary: Install existing packages
prepare:
  - how: install
    package:
      - tree
      - diffutils
adjust:
  - when: distro == ubuntu,debian
    prepare+<:
      - how: shell
        script: apt-get update
